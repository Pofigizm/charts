1 - Какая разница между get и post?
через get данные можно передать только в строке запроса, а при помощи post еще есть тело запроса

2 - В чем разница между .call и .apply
call принимает аргументы через запятую, а apply массивом 

3 - function Foo(){};
в чем разница между Foo() и new Foo()
Вызов функции и создание нового инстанса "класса"

4 - Задание (на пару минут)
var a = [1,2,3], b = [4,5,6], c = [7,8,9,10], d = [a,b,c];
Сделайте массив d плоским. (чтобы он содержал только числа)
d = d.reduce((e, f) => e.concat(f), []);

5 - Что такое "reset" CSS и для чего он нужен?
это набор css правил, служит для сброса состояния всех элементов

6 - Какими CSS пре/пост процессорами пользовались, какой больше нравится?
Less, PostCss. Нравится PostCss.

7 - Для чего нужен redux в проектах на реакте?
Для управления состоянием приложения

8 - Какими сборщиками проекта пользовались? 
grunt, gulp, webpack
